events:
  schema:
    validation:
      strict: true

  kafka:
    producer:
      acks: all
      retries: 5
      compression-type: lz4
    consumer:
      concurrency: 10
      max-poll-records: 1000

management:
  tracing:
    sampling:
      probability: 0.1  # Sample 10% in production